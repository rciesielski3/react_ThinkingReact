# Vite Upgrade Archive

This file preserves notes and branch references for the staged Vite upgrade work.

- Branches created during upgrade: `upgrade/vite-6`, `upgrade/vite-7`, `upgrade/vite-8`.
- Final merge: `upgrade/vite-8` merged into `main` on 2026-08-12.
- Rationale: Upgraded sequentially to catch compatibility issues with `@vitejs/plugin-react` and `esbuild`.
- Local note: contributors using Node <20 may see build failures; CI validated Vite 8 on Node 20.

If you want these working branches preserved, I can tag them or move them to a `archive/` prefix.
